{
    "private": true,
    "workspaces": [
        "@scout/server",
        "@scout/client",
        "@scout/shared"
    ],
    "scripts": {
        "dev": "concurrently -n client,server -c blue,green \"yarn --cwd client dev\" \"yarn --cwd server dev\""
    },
    "devDependencies": {
        "concurrently": "^7.6.0"
    }
}
